<li>
	{{burger_name}}

	
		{{#if devoured}}
		<button class="change-devour mySecondBtn" data-id="{{id}}" data-devoured="{{devoured}}">Devour it!</button>
		{{/if}}
	</button>
{{!-- 
	<button class="delete-cat" data-id="{{this.id}}">Ate!</button> --}}
</li>